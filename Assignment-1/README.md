# Task 1
This program displays the values of mathematical operations (addition, subtraction, multiplication, division) on two user-entered numbers.

The program first takes 2 inputs from user, converts them to the float data-type. Then, it displays the output after performing the mathematical operations.

It uses input function to take user input, then displays out using print function and f-strings.
